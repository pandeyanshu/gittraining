git (version control system )
git keep of all log for all the lines changed

it also keep track of what and who made the 
changes

command

git status 

git add  file name  (changes made but snapshot\ log not 
           created )

git add . 
(to add all the changes)

git commit -m "message" (changes are named)

git diff (to see all changes)


git config global user.email "axxx@gmail.com"
git config global user.name 


git log  (to check the changes )

git checkout <commit id>
  (help us to see what chages are made in the
   commit or move the head to the given commit)

git checkout master
(to switch to master)

git branch
(will give the name of the branches )


 git checkout -b <branch name> 
(this creates a new branch from the current branch 
coping the current branch code)



git merge <branch name> 
(this is done to merge the branch )

git branch -d <branch name> 
(to delete a branch)


conflict 
